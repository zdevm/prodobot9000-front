import { HumanizeTriggerPipe } from './humanize-trigger.pipe';

describe('HumanizeTriggerPipe', () => {
  it('create an instance', () => {
    const pipe = new HumanizeTriggerPipe();
    expect(pipe).toBeTruthy();
  });
});
