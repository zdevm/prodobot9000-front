import { AsyncButtonDirective } from './async-button.directive';

describe('AsyncButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AsyncButtonDirective();
    expect(directive).toBeTruthy();
  });
});
